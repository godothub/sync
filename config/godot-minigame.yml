name: Sync MiniGame

env:
  SOURCE_GITHUB_REPO: godothub/godot-minigame
  TARGET_ATOMGIT_REPO: godothub/godot-minigame
  MAX_SYNC_FILES: 100
  START_RELEASE_TAG: ''
  SKIP_RELEASES_BEFORE_START: false
  SYNC_BRANCHES: true
  SYNC_TAGS: true
  SYNC_RELEASES: true

triggers:
  schedule: false
  schedule_cron: []
  workflow_dispatch: true
